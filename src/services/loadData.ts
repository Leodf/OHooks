import header from '../mocks/header';
import produtores from '../mocks/produtores';

export const loadProdutores = () => {
  return produtores;
};

export const loadHeader = () => {
  return header;
};
